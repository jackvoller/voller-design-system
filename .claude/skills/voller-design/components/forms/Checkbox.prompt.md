Multi-select and consent rows.

```jsx
<Checkbox label="Send me release notes" checked={on} onChange={e => setOn(e.target.checked)} />
```

Controlled — pass `checked` and `onChange`. 6px radius (tighter than controls; the box is small). The tick is the same checkmark shape as the Meal Planner icon.
