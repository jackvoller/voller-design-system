Choice from a known list; native `<select>` underneath so mobile gets the system picker.

```jsx
<Select label="App" options={['UnPickle', 'Meal Planner', 'UnJumble', 'Riverly']} />
```

Matches Input's geometry exactly (10px/14px padding, 10px radius) so the two line up in a row.
